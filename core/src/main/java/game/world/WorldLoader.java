package game.world;

/** Loads the game world and its assets. */
public class WorldLoader {
    /**
     * Initialize world assets. Implementation pending.
     */
    public void load() {
        // Implementation will load world data
    }
}
